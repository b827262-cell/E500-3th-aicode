[project]
name = "gpt-codex-bridge"
version = "2.0.0"
description = "A persistent, single-worker Telegram adapter for sandboxed Codex jobs"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27,<1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
